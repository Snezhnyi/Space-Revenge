using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurrentRoomScript : MonoBehaviour
{
    public int currentRoom;
    // Start is called before the first frame update
    void Start()
    {
        currentRoom = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
